<%= form_for(current_user.active_relationships.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :followed_id, @manga.id %></div>
    <%= f.submit t(".follow"), class: "btn btn-primary" %>
  <div>
    <%= t(".followed") %> : <%= @manga.followers.count %> <%= t(".users") %>
  </div>
<% end %>
