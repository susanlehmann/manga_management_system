<ul class="list-group">
  <li class="list-group-item d-flex justify-content-between align-items-center">
    <%= link_to category.name, category_path(category) %>
    <span class="badge badge-primary badge-pill"><%= category.mangas.count %></span>
  </li>
</ul>
